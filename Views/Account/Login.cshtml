@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - Dark Cloud</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            background: linear-gradient(to bottom, #0f0f0f, #1a1a1a);
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .navbar {
            background-color: #121212;
            padding: 1rem 2rem;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
        }
        .navbar-brand {
            color: #f5f5f5;
            font-size: 1.8rem;
            font-weight: bold;
            text-decoration: none;
            display: flex;
            align-items: center;
        }
        .navbar-brand i {
            margin-right: 0.5rem;
            color: #00aeef;
        }
        .login-container {
            max-width: 400px;
            margin: auto;
            padding: 2rem;
            background: #1e1e1e;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
            color: #f5f5f5;
        }
        .login-container h2 {
            text-align: center;
            margin-bottom: 1.5rem;
            color: #00aeef;
        }
        .login-container .form-label {
            color: #f5f5f5;
        }
        .login-container .input-group {
            margin-bottom: 1rem;
        }
        .login-container .input-group-text {
            background-color: #333;
            border: none;
            color: #00aeef;
        }
        .login-container .form-control {
            background-color: #2a2a2a;
            border: none;
            color: #f5f5f5;
        }
        .login-container button {
            background-color: #00aeef;
            border: none;
            padding: 0.75rem;
            color: white;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            transition: background-color 0.3s ease;
        }
        .login-container button:hover {
            background-color: #007bbf;
        }
        footer {
            background-color: #121212;
            color: #f5f5f5;
            text-align: center;
            padding: 1rem 0;
            margin-top: auto;
            width: 100%;
        }
        footer a {
            color: #00aeef;
            text-decoration: none;
        }
        footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <a href="/" class="navbar-brand">
            <i class="fas fa-gamepad"></i> DARK CLOUD
        </a>
    </nav>
    <div class="login-container">
        <h2>Iniciar Sesión</h2>
        <form method="post" action="/Account/Login">
            <div class="input-group mb-3">
                <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                <input type="email" class="form-control" name="email" placeholder="Correo Electrónico" required>
            </div>
            <div class="input-group mb-3">
                <span class="input-group-text"><i class="fas fa-lock"></i></span>
                <input type="password" class="form-control" name="password" placeholder="Contraseña" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Entrar</button>
        </form>
        <p class="text-center mt-3">¿No tienes una cuenta? <a href="/Account/Register" style="color: #007bff;">Regístrate aquí</a></p>
    </div>
    <footer>
        &copy; 2025 - DarkCloud. Todos los derechos reservados.
    </footer>
</body>
</html>